"""Terminal monitor UI for the WorldQuant workflow.

The UI package is intentionally independent from the workflow runner.  It only
observes runtime files and writes its own cache/log artifacts.
"""

